const axios = require('axios');
const mongoose = require('mongoose');

async function test() {
  try {
    // We need a valid token. Let's just create a dummy script or look at the DB
  } catch (err) {
    console.error(err);
  }
}
test();
